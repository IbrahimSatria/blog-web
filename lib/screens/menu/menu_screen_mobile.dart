import 'package:flutter/material.dart';

class MenuScreenMobilePortrait extends StatelessWidget {
  const MenuScreenMobilePortrait({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class MenuScreenMobileLandscape extends StatelessWidget {
  const MenuScreenMobileLandscape({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
