import 'package:flutter/material.dart';

class MenuScreenDesktop extends StatelessWidget {
  const MenuScreenDesktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: [Text('Testing'), Text('Testing')],
      ),
    );
  }
}
