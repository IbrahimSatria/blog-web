import 'package:flutter/material.dart';

class MenuScreenTabletPortrait extends StatelessWidget {
  const MenuScreenTabletPortrait({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class MenuScreenTabletLandscape extends StatelessWidget {
  const MenuScreenTabletLandscape({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
